@model IEnumerable<Ignis.Models.Vacancy>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FireStation.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rank.RankName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vacant)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FireStation.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rank.RankName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Vacant)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id })
        </td>
    </tr>
}

</table>
